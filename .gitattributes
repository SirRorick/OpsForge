# Normalise line endings so Windows checkouts don't change file bytes.
* text=auto eol=lf

# Map files must survive byte for byte. They are minified JSON with no trailing
# newline, and any line-ending translation would corrupt them.
reference/** -text -diff
reference/**/*.md text eol=lf

*.png binary
*.jpg binary
*.glb binary
